name: shelf_isar
description: A server app using the shelf package and Docker.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.17.5 <3.0.0'

dependencies:
  # Dart
  args: ^2.0.0

  # Shelf server
  shelf: ^1.3.1
  shelf_router: ^1.1.3
  shelf_hotreload: ^1.3.0
  shelf_cors_headers: ^0.1.3

  # Database
  isar: ^3.0.0-dev.6

dev_dependencies:
  http: ^0.13.0
  lints: ^2.0.0
  test: ^1.15.0

  # Generators
  isar_generator: ^3.0.0-dev.6
  build_runner: ^2.1.11
